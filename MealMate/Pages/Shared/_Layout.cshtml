<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - MealMate</title>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Manrope:wght@300;400;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/MealMate.styles.css" asp-append-version="true" />
</head>
<body class="app-shell">
    <header class="app-header">
        <div class="app-brand">
            <span class="app-logo">🍽️</span>
            <div>
                <a class="app-title" asp-area="" asp-page="/Index">MealMate</a>
                <p class="app-subtitle">Планируйте вкусный день без лишних усилий</p>
            </div>
        </div>
        <button class="nav-toggle" aria-label="Открыть меню" aria-expanded="false">
            <span></span>
            <span></span>
            <span></span>
        </button>
        <nav class="app-nav">
            <a class="nav-link" asp-page="/Index">Дашборд</a>
            <a class="nav-link" asp-page="/Dishes/Index">Блюда</a>
            <a class="nav-link" asp-page="/MealGroups/Index">Группы блюд</a>
            <a class="nav-link" asp-page="/Products/Index">Продукты</a>
        </nav>
    </header>

    <div class="app-container">
        <main role="main" class="app-main">
            @RenderBody()
        </main>
    </div>

    <footer class="app-footer">
        <p>© @DateTime.Now.Year MealMate. Всё для домашнего уюта.</p>
    </footer>

    <script src="~/js/site.js" asp-append-version="true"></script>
    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>
